# slot-b env template
# Source of truth for Phase C Railway Variables.
# Each clone gets 6 env vars (per multiplexer contract).

# === Clone A: agent-fabric-oversight ===
# (canonical slug; legacy source spec name agent-fabric__human-oversight-integration)
CLONE_A_ID=agent-fabric-oversight
CLONE_A_MANIFEST_PATH=clone_specs/agent-fabric__human-oversight-integration.json
CLONE_A_CATEGORY=agent_marketplace
CLONE_A_GAP_NAME=Audit-ready technical documentation
CLONE_A_REGULATORY_ANCHOR=EU AI Act
CLONE_A_PRICE_USD=0.05

# === Clone B: agent-ledger ===
CLONE_B_ID=agent-ledger
CLONE_B_MANIFEST_PATH=clone_specs/agent-ledger.json
CLONE_B_CATEGORY=agent_devtools
CLONE_B_GAP_NAME=Data governance
CLONE_B_REGULATORY_ANCHOR=NIST AI RMF
CLONE_B_PRICE_USD=0.05

# === Per-slot (Railway Variables) ===
# ROOT_DIRECTORY=services/slot-b
# DEPLOYMENT_AT=<set-by-CI>

# slot-a env template
# Source of truth for Phase C Railway Variables.
# Each clone gets 6 env vars (per multiplexer contract in
# A2A-Meta/docs/clone_repo_architecture_v0_DRAFT.md §6.1).
# category / gap_name / regulatory_anchor / price_usd values
# are derived from each clone's clone_specs/<slug>.json.

# === Clone A: agent-lens ===
CLONE_A_ID=agent-lens
CLONE_A_MANIFEST_PATH=clone_specs/agent-lens.json
CLONE_A_CATEGORY=agent_devtools
CLONE_A_GAP_NAME=Cross-Stack Provenance & Audit Artifact Generation
CLONE_A_REGULATORY_ANCHOR=NIST AI RMF
CLONE_A_PRICE_USD=0.05

# === Clone B: audit-bazaar ===
CLONE_B_ID=audit-bazaar
CLONE_B_MANIFEST_PATH=clone_specs/audit-bazaar.json
CLONE_B_CATEGORY=agent_devtools
CLONE_B_GAP_NAME=...
CLONE_B_REGULATORY_ANCHOR=...
CLONE_B_PRICE_USD=0.05

# === Per-slot (Railway Variables) ===
# ROOT_DIRECTORY=services/slot-a
# DEPLOYMENT_AT=<set-by-CI>

# slot-c env template
# Source of truth for Phase C Railway Variables.

# === Clone A: compliance-lens ===
CLONE_A_ID=compliance-lens
CLONE_A_MANIFEST_PATH=clone_specs/compliance-lens.json
CLONE_A_CATEGORY=agent_governance
CLONE_A_GAP_NAME=Cross-regulatory compliance
CLONE_A_REGULATORY_ANCHOR=NIST AI RMF
CLONE_A_PRICE_USD=0.05

# === Per-slot (Railway Variables) ===
# ROOT_DIRECTORY=services/slot-c
# DEPLOYMENT_AT=<set-by-CI>
#
# === Capacity for 2 more clones (when promoted from backlog) ===
# Add CLONE_B_ID, CLONE_B_MANIFEST_PATH, etc. when a backlog
# clone is promoted and assigned to slot-c.
